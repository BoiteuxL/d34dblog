<footer>
    <div class="container mx-auto py-4">
        <div class="text-center text-sm text-shadow-neutral-50">
            {{ languages[page.lang].availableText }}
            {% for link in page.url | locale_links %}
                {%- if not loop.first %}/{% endif %}
                <a href="{{ link.url }}" lang="{{ link.lang }}" hreflang="{{ link.lang }}">{{ link.label }}</a>
            {% endfor %}
        </div>
{#         <p class="text-center text-sm text-shadow-neutral-50">
            Built with 11ty on {{ site.buildDate }}
        </p>
        <p class="text-center text-sm text-shadow-neutral-50">
            Source code available
            <a href="https://github.com/BoiteuxL/d34dblog" target="_blank" rel="noopener noreferrer">here</a>
        </p> #}
    </div>
</footer>